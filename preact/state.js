// unused
export const state = {
    tickboxes: new Map(), // Map<id: string, value: boolean|null>
    inputs: new Map(),    // Map<id: string, value: string>
    tooltip: null         // string|null (ID of active tooltip)
  };
  