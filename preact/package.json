{
  "name": "14kb-spa-preact",
  "version": "1.0.0",
  "main": "app.jsx",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cp index.html build/ && esbuild app.jsx --bundle --outfile=build/app.js --loader:.js=jsx --jsx-factory=h --jsx-fragment=Fragment --servedir=build",
    "dev": "cp index.html build/ && esbuild app.jsx --bundle --outfile=build/app.js --loader:.js=jsx --jsx-factory=h --jsx-fragment=Fragment --servedir=build --watch"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.25.3"
  },
  "dependencies": {
    "preact": "^10.26.5"
  }
}
